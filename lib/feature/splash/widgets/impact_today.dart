import 'package:flutter/material.dart';

class ImpactToday extends StatelessWidget {
  const ImpactToday({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Impact Today Widget', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
